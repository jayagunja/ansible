- name: loops
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "{{ items }}"
    loop:
    - linux
    - shellscript
    - ansible
    - terraform

